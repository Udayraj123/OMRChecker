# Print number of files found
# Confirmation before overwrite
# Input directory
# Output directory

"""Note: Skip corrupt images and print their path in output.

--replace flag (false by default, overwrites if same ext, deletes if diff ext)
--trigger-size [0]
--max-width
--max-height
--outputExtension ().
"""
